# Ensure Gradle wrapper scripts keep correct line endings
gradlew text eol=lf
gradlew.bat text eol=crlf

# Ensure binary files are treated as binary to prevent corruption during export
*.jar binary
*.png binary
*.webp binary
*.jpg binary
*.jpeg binary
*.keystore binary
